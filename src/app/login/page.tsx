import LoginForm from "@/components/loginForm";

const LoginPage = () => {
    return (
        <div className="login">
            <h1>Welcome to the LoginPage</h1>
            <LoginForm />
        </div>
    );
};

export default LoginPage;
